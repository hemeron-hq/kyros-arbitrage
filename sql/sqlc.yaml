version: "2"
sql:
  - engine: "sqlite"
    schema: "migrations/001_init.sql"
    queries: "queries/history.sql"
    gen:
      go:
        package: "db"
        out: "../gen/db"
        emit_json_tags: true
